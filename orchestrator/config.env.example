# Orchestrator configuration
# Copy to config.env and fill in values
# The orchestrator sources this file and exports all vars before launching Claude Code.
# The MCP server subprocess inherits these env vars automatically.

# Dashboard API
DASHBOARD_URL=http://localhost:3000
DASHBOARD_API_KEY=your_dashboard_api_key

# Workspace paths
WORKSPACE_DIR=/home/deploy/workspace/project-tracker
ERP_REPO=${WORKSPACE_DIR}/erp
COMMPORTAL_REPO=${WORKSPACE_DIR}/commportal-v2
TRACKER_REPO=${WORKSPACE_DIR}/project-tracker

# Mailgun (email notifications)
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=mail.example.com
MAILGUN_FROM=agent@mail.example.com
MAILGUN_TO=you@example.com

# Notification email (used by AdminNotifier in the dashboard)
NOTIFICATION_EMAIL=you@example.com

# Twilio (SMS notifications)
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM=+10000000000
TWILIO_TO=+10000000000

# Honeybadger (optional — auto-creates projects from production errors)
HONEYBADGER_AUTH_TOKEN=your_honeybadger_token
# Space-separated Honeybadger project IDs
HONEYBADGER_PROJECT_IDS=
# Comma-separated mapping: hb_project_id:repo_name:display_name
HONEYBADGER_PROJECT_MAP=

# Hatchbox (optional — staging deploys)
HATCHBOX_API_TOKEN=your_hatchbox_token
HATCHBOX_ERP_WEBHOOK=https://app.hatchbox.io/webhooks/deployments/YOUR_TOKEN
HATCHBOX_COMMPORTAL_WEBHOOK=https://app.hatchbox.io/webhooks/deployments/YOUR_TOKEN

# GitHub Webhooks (optional — auto-creates replies from PR reviews)
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret

# Claude Code auth (uncomment when switching from Max to API)
# ANTHROPIC_API_KEY=your_anthropic_api_key
